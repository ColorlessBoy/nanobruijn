# 合取世界：从构造到分解
world And
title 合取世界：从构造到分解
intro 你面前是合取的世界。目标是以 a → b → a ∧ b 为起点的所有通路：构造它（And.intro），拆开它（And.right / And.left，以及 cases）。
requires: TrueFalse
lesson: And a b 读作"a 且 b"：它的证据是一对证据——a 的一份、b 的一份，捆在一起。
lesson: 构造用 And.intro：手里有 ha : a 和 hb : b 时，And.intro 给出 And a b。拆开用投影：And.left 取左半、And.right 取右半。
lesson: cases 是投影的合体：对 h : And a b 做 cases h as ha hb，两个分量同时进入上下文。上一关学过的函数推理（Basic 世界）在这里照常工作——命题逻辑就搭在函数类型上。
example: forall (a : Prop), a -> And a a
intro a
intro ha
apply And.intro
exact ha
exact ha
using: and
level 1
name 初见合取
goal: forall (a : Prop), forall (b : Prop), a -> b -> And a b
hint: 目标是函数类型（∀/->）时先 intro 拆开：intro a、intro b、intro ha、intro hb
hint: 现在目标只剩 And a b：apply And.intro 拆成 a 和 b 两个子目标
hint: 两个子目标分别 exact ha、exact hb
solution:
intro a
intro b
intro ha
intro hb
apply And.intro
exact ha
exact hb
variant: 证交换版 a -> b -> And b a（只换 exact 顺序）
---
level 2
name 只取右半
goal: forall (a : Prop), forall (b : Prop), And a b -> b
ban: cases
hint: 结论是 b，但前提是 And a b。试试投影 And.right
hint: intro 引入 h : And a b 后，用 exact And.right a b h 一步收工（And.right 是"取右半"的投影）
solution:
intro a
intro b
intro h
exact And.right a b h
---
level 3
name 结合律
goal: forall (a : Prop), forall (b : Prop), forall (c : Prop), And (And a b) c -> And a (And b c)
hint: 前提 And (And a b) c 先 intro h 引入，再 cases h 得到 hab : And a b 与 hc : c——目标保持 And a (And b c)
hint: 目标 And a (And b c) 里的嵌套前提也需要拆：cases hab 得到 ha : a 与 hb : b（hc 仍在作用域内）
hint: 现在 apply And.intro 拆目标为 a 和 And b c：前者 exact ha，后者再 apply And.intro 用 hb / hc
solution:
intro a
intro b
intro c
intro h
cases h as hab hc
cases hab as ha hb
apply And.intro
exact ha
apply And.intro
exact hb
exact hc
variant: 证反向结合律 And a (And b c) -> And (And a b) c（结构对称照搬）
---
level 4
name 交换律
goal: forall (a : Prop), forall (b : Prop), And a b -> And b a
hint: 和上一关同样的模式：apply And.intro 拆目标，cases 拆前提
solution:
intro a
intro b
intro h
cases h as ha hb
apply And.intro
exact hb
exact ha
---
level 5
name 推广到一半
goal: forall (a : Prop), forall (b : Prop), forall (c : Prop), (a -> b) -> And a c -> And b c
hint: intro 拆开后手里有 fab : a -> b 和 h : And a c——目标是 And b c
hint: cases h as ha hc 把前提拆成两份证据
hint: apply And.intro 拆目标：b 用 exact fab ha，c 用 exact hc
solution:
intro a
intro b
intro c
intro fab
intro h
cases h as ha hc
apply And.intro
exact fab ha
exact hc
variant: 证 (b -> c) -> And a b -> And a c（拆另一半）
