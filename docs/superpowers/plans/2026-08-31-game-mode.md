# GAME 模式实施计划（关卡闯关式教学 + opencode skill 集成）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** REPL 新增 GAME 模式（纯文本 `.game` 关卡 + 世界进度/星级/存档 + 关卡内 hint/ban/标准解），playbook.md 重写为游戏设计文档，并交付 opencode skill（AI 可经 `--script` 驱动 REPL）。

**Architecture:** 独立 `teaching/game.py`（GameLoader 解析 + GameSession 状态机，仿 fol.py 逐行风格）；repl.py 复用现有 `_run_proof` 子循环加 level/game 包装（`_GameSession` 信号，与 `_ProveSession` 同模式）；`worlds/*.game` 纯文本数据；`--script/--json/--game` 补 CLI 非交互通道；`.opencode/skills/nanobruijn` + `.opencode/agents/nanobruijn-tutor.md` 交付 AI 集成。

**Tech Stack:** Python 3.10+（py310 兼容，line-length 100）、pytest、ruff；无新依赖。

**Spec:** `docs/superpowers/specs/2026-08-31-game-mode-design.md`

## Global Constraints

- 内核零改动：不修改 `tc_*.py`/`dag.py`/`env.py`/NDJSON `parser.py`/`expr.py`/`proof.py` 的洞模型——只加 `game.py`、改 `repl.py`/`__main__.py`/`tactics.py`（ban 检查在 repl 层，tactics.py 不动）
- 类型错误抛 `ValueError`；`#prove` 现有行为不变（游戏是它的包装层）
- 教学语法：binder 必须带类型注解；隐式参数用 `@Const`；`Exists` 需 `@Exists.{1} Prop p`
- 通关判定只要求内核检查通过（与标准解不做 def_eq 比对）
- 提交前 `pytest py_nanobruijn -q` 全绿 + `ruff check py_nanobruijn` 0 errors
- 中文错误消息/教学文本；`saves/` 加入 .gitignore

## 文件结构

| 文件 | 职责 |
|---|---|
| `py_nanobruijn/teaching/game.py`（新建） | GameLoader（.game 解析）+ Game/Level 数据结构 + GameSession（星级/解锁/存档） |
| `py_nanobruijn/worlds/*.game`（新建 6 个） | 世界内容（And/Or/Not/Exists/Iff/Combo，各 5 关） |
| `py_nanobruijn/teaching/repl.py`（修改） | `#game`/`#worlds` 命令、`_GameSession` 信号、关卡循环（hint/solution/ban/步数/星级）、`--script` 支持 |
| `py_nanobruijn/__main__.py`（修改） | `--script`/`--json`/`--game` 参数 |
| `py_nanobruijn/test_teaching.py`（修改） | TestGameLoader/TestGameSession/TestWorldContent/TestWorldSolutions/TestGameRepl + TestCli 扩展 |
| `tutorials/prove-playbook.md`（重写） | 游戏设计文档（三节总览 + 每世界一章 + 加关卡指南） |
| `.opencode/skills/nanobruijn/SKILL.md`（新建） | AI 调用规则（`--script` 驱动 REPL） |
| `.opencode/agents/nanobruijn-tutor.md`（新建） | 批改 subagent（bash 只放行 py-nanobruijn） |
| `AGENTS.md`/`teaching/README.md`（修改） | 架构与使用文档更新 |
| `.gitignore`（修改） | 加 `saves/` |

---

### Task 1: GameLoader 解析器 + 数据结构

**Files:**
- Create: `py_nanobruijn/teaching/game.py`
- Test: `py_nanobruijn/test_teaching.py`（新增 `class TestGameLoader`）

**Interfaces:**
- Consumes: 无（纯文本解析，零依赖）
- Produces: `GameLoader.load(path: str) -> Game`；`Game(world_id, title, intro, levels)`；`Level(number: int, name: str, goal: str, hints: list[str], solution: list[str], bans: list[str])`

- [ ] **Step 1: 写失败测试**

```python
class TestGameLoader:
    """game.py：.game 纯文本解析。"""

    def _write(self, tmp_path, text):
        p = tmp_path / "and.game"
        p.write_text(text, encoding="utf-8")
        return str(p)

    def test_parse_minimal(self, tmp_path):
        path = self._write(tmp_path, (
            "world And\n"
            "title 合取世界\n"
            "intro 欢迎\n"
            "level 1\n"
            "name 初见合取\n"
            "goal: forall (a : Prop), forall (b : Prop), a -> b -> And a b\n"
            "hint: 目标头部是 And\n"
            "ban: cases\n"
            "solution:\n"
            "intro a\n"
            "intro b\n"
            "apply And.intro\n"
        ))
        from py_nanobruijn.teaching.game import GameLoader
        g = GameLoader().load(path)
        assert g.world_id == "And"
        assert g.title == "合取世界"
        assert len(g.levels) == 1
        lv = g.levels[0]
        assert lv.number == 1 and lv.name == "初见合取"
        assert "And a b" in lv.goal
        assert lv.hints == ["目标头部是 And"]
        assert lv.solution == ["intro a", "intro b", "apply And.intro"]
        assert lv.bans == ["cases"]

    def test_multiple_levels(self, tmp_path):
        path = self._write(tmp_path, (
            "world Or\n"
            "title 析取世界\n"
            "level 1\n"
            "goal: forall (a : Prop), a -> Or a b\n"
            "---\n"
            "level 2\n"
            "goal: forall (a : Prop), Or a b -> b\n"
        ))
        from py_nanobruijn.teaching.game import GameLoader
        g = GameLoader().load(path)
        assert [lv.number for lv in g.levels] == [1, 2]

    def test_missing_world(self, tmp_path):
        path = self._write(tmp_path, "title 没有 world 字段\nlevel 1\ngoal: Prop\n")
        from py_nanobruijn.teaching.game import GameLoader
        with pytest.raises(ValueError, match="world"):
            GameLoader().load(path)

    def test_missing_goal(self, tmp_path):
        path = self._write(tmp_path, "world And\nlevel 1\nname 无目标\n")
        from py_nanobruijn.teaching.game import GameLoader
        with pytest.raises(ValueError, match="goal"):
            GameLoader().load(path)

    def test_bad_ban(self, tmp_path):
        path = self._write(tmp_path, (
            "world And\nlevel 1\ngoal: Prop\nban: warp\n"))
        from py_nanobruijn.teaching.game import GameLoader
        with pytest.raises(ValueError, match="ban.*warp"):
            GameLoader().load(path)

    def test_hint_order_and_comment(self, tmp_path):
        path = self._write(tmp_path, (
            "world And\n"
            "level 1\n"
            "goal: Prop\n"
            "# 注释行\n"
            "hint: 第一\n"
            "hint: 第二\n"
        ))
        from py_nanobruijn.teaching.game import GameLoader
        g = GameLoader().load(path)
        assert g.levels[0].hints == ["第一", "第二"]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest py_nanobruijn/test_teaching.py::TestGameLoader -q`
Expected: FAIL（`ModuleNotFoundError: py_nanobruijn.teaching.game`）

- [ ] **Step 3: 实现 game.py（解析器 + 数据结构）**

```python
"""game 模式：.game 纯文本关卡语言加载器 + 游戏状态机。

关卡格式（仿 core.fol 逐行风格，零依赖）：

    world And
    title 合取世界
    intro 开场叙事
    level 1
    name 关卡名
    goal: <命题（教学语法）>
    hint: <分层提示，可多个>
    ban: <禁用的 tactic 名，可多个>
    solution:
    <标准解脚本行，到 --- / level / 文件尾为止>
    ---
    level 2
    ...
"""
from __future__ import annotations

import json
import os
import re

KNOWN_TACTICS = ('intro', 'apply', 'exact', 'cases', 'done',
                 'abort', 'context', 'help')

_FIELD_RE = re.compile(r'^([a-zA-Z]+):?\s*(.*)$')


class Level:
    def __init__(self, number: int, name: str, goal: str,
                 hints: list[str], solution: list[str],
                 bans: list[str]):
        self.number = number
        self.name = name
        self.goal = goal
        self.hints = hints
        self.solution = solution
        self.bans = bans


class Game:
    def __init__(self, world_id: str, title: str, intro: str,
                 levels: list[Level]):
        self.world_id = world_id
        self.title = title
        self.intro = intro
        self.levels = levels

    def level(self, number: int) -> Level:
        return self.levels[number - 1]


class GameLoader:
    def load(self, path: str) -> Game:
        with open(path, encoding="utf-8") as f:
            lines = f.read().splitlines()
        world_id = title = intro = ""
        levels: list[Level] = []
        cur: dict | None = None
        for lineno, line in enumerate(lines, 1):
            stripped = line.strip()
            if not stripped or stripped.startswith('#'):
                continue
            if stripped == '---':
                cur = None
                continue
            m = _FIELD_RE.match(stripped)
            if m is None:
                raise ValueError(f"game: 无法解析第 {lineno} 行: {stripped!r}")
            key, val = m.group(1), m.group(2).strip()
            if key == 'world':
                world_id = val
            elif key == 'title':
                title = val
            elif key == 'intro':
                intro = val
            elif key == 'level':
                cur = {'number': int(val), 'name': '', 'goal': '',
                       'hints': [], 'solution': [], 'bans': []}
                levels.append(cur)
            elif cur is None:
                raise ValueError(f"game: 第 {lineno} 行 {key} 在关卡外: {stripped!r}")
            elif key == 'name':
                cur['name'] = val
            elif key == 'goal':
                cur['goal'] = val
            elif key == 'hint':
                cur['hints'].append(val)
            elif key == 'ban':
                if val not in KNOWN_TACTICS:
                    raise ValueError(f"game: 第 {lineno} 行 ban 了未知 tactic {val!r}")
                cur['bans'].append(val)
            elif key == 'solution':
                pass  # 后续行属于 solution
            else:
                raise ValueError(f"game: 第 {lineno} 行未知字段 {key!r}")
        if not world_id:
            raise ValueError("game: 缺少 world 字段")
        for lv in levels:
            if not lv['goal']:
                raise ValueError(f"game: level {lv['number']} 缺少 goal")
        return Game(world_id, title, intro,
                    [Level(l['number'], l['name'], l['goal'], l['hints'],
                           l['solution'], l['bans']) for l in levels])
```

**注意**：`solution:` 行的后续内容怎么进 `cur['solution']`？当前 `_FIELD_RE` 把 `solution:` 的后续行当作新字段解析会报未知字段。**修法**：在 `key == 'solution'` 时进入"收集模式"——后续行无条件追加到 `cur['solution']`，直到 `---`/`level`/文件尾。在 Step 3 的实现里补这个状态（`in_solution: bool`），Step 1 的 `test_parse_minimal` 会验证（solution 3 行）。

```python
        # 修正版解析循环（替换上面 for 内的分支逻辑）：
        in_solution = False
        for lineno, line in enumerate(lines, 1):
            stripped = line.strip()
            if not stripped or stripped.startswith('#'):
                continue
            if in_solution:
                if stripped == '---':
                    in_solution = False
                    cur = None
                    continue
                if stripped.startswith('level '):
                    in_solution = False
                else:
                    cur['solution'].append(stripped)
                    continue
            ...
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest py_nanobruijn/test_teaching.py::TestGameLoader -q`
Expected: PASS（6 passed）

- [ ] **Step 5: Commit**

```bash
git add py_nanobruijn/teaching/game.py py_nanobruijn/test_teaching.py
git commit -m "feat(teaching): GameLoader parsing for .game level files"
```

---

### Task 2: GameSession 状态机（星级/解锁/存档）

**Files:**
- Modify: `py_nanobruijn/teaching/game.py`（追加 GameSession）
- Test: `py_nanobruijn/test_teaching.py`（新增 `class TestGameSession`）

**Interfaces:**
- Consumes: Task 1 的 `GameLoader`/`Game`/`Level`
- Produces: `GameSession(game: Game, saves_dir: str | None = None)`；
  `stars: dict[int, int]`；`current_level_no: int`；
  `next_unfinished() -> int | None`；`unlocked(number: int) -> bool`；
  `complete(steps: int, used_hint: bool) -> int`（返回星级，内部存档）；
  `save()`；`load_progress()`（实例内）

- [ ] **Step 1: 写失败测试**

```python
class TestGameSession:
    """game.py：星级 / 解锁 / 存档。"""

    def _game(self):
        from py_nanobruijn.teaching.game import Game, GameLoader, Level
        return Game("And", "合取世界", "intro",
                    [Level(1, "L1", "goal1", [], ["intro a"], []),
                     Level(2, "L2", "goal2", [], ["intro b"], []),
                     Level(3, "L3", "goal3", [], ["intro c"], [])])

    def test_stars_1_to_3(self, tmp_path):
        from py_nanobruijn.teaching.game import GameSession
        s = GameSession(self._game(), saves_dir=str(tmp_path))
        assert s.complete(steps=10, used_hint=True) == 1   # 用过 hint
        assert s.complete(steps=10, used_hint=False) == 2  # 没用 hint
        assert s.complete(steps=1, used_hint=False) == 3   # 步数 <= 标准解
        assert s.stars == {1: 3}

    def test_unlock_sequential(self):
        from py_nanobruijn.teaching.game import GameSession
        s = GameSession(self._game(), saves_dir=None)
        assert s.unlocked(1)
        assert not s.unlocked(2)
        s.complete(1, False)
        assert s.unlocked(2)
        assert not s.unlocked(3)
        assert s.next_unfinished() == 2

    def test_next_unfinished_all_done(self):
        from py_nanobruijn.teaching.game import GameSession
        s = GameSession(self._game(), saves_dir=None)
        for i in range(3):
            s.complete(1, False)
        assert s.next_unfinished() is None

    def test_progress_persists(self, tmp_path):
        from py_nanobruijn.teaching.game import GameSession
        s1 = GameSession(self._game(), saves_dir=str(tmp_path))
        s1.complete(2, False)  # level 1, 2 星
        s1.complete(1, True)   # level 2, 1 星
        s2 = GameSession(self._game(), saves_dir=str(tmp_path))
        s2.load_progress()
        assert s2.stars == {1: 2, 2: 1}
        assert s2.next_unfinished() == 3
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest py_nanobruijn/test_teaching.py::TestGameSession -q`
Expected: FAIL（`ModuleNotFoundError: GameSession`）

- [ ] **Step 3: 实现 GameSession**

```python
SAVES_DIR = os.path.join(os.path.dirname(os.path.dirname(__file__)), "saves")


class GameSession:
    def __init__(self, game: Game, saves_dir: str | None = None):
        self.game = game
        self.saves_dir = saves_dir if saves_dir is not None else SAVES_DIR
        self.stars: dict[int, int] = {}
        self.current_level_no: int | None = None

    def unlocked(self, number: int) -> bool:
        return number == 1 or (number - 1) in self.stars

    def next_unfinished(self) -> int | None:
        for lv in self.game.levels:
            if lv.number not in self.stars:
                return lv.number
        return None

    def complete(self, steps: int, used_hint: bool) -> int:
        lv = self.game.level(self.current_level_no)
        stars = 1
        if not used_hint:
            stars = 2
        if not used_hint and steps <= len(lv.solution):
            stars = 3
        self.stars[lv.number] = stars
        self.save()
        return stars

    def save(self) -> None:
        os.makedirs(self.saves_dir, exist_ok=True)
        path = os.path.join(self.saves_dir, f"{self.game.world_id}.json")
        with open(path, "w", encoding="utf-8") as f:
            json.dump({"stars": {str(k): v for k, v in self.stars.items()}},
                      f, ensure_ascii=False)

    def load_progress(self) -> None:
        path = os.path.join(self.saves_dir, f"{self.game.world_id}.json")
        if not os.path.exists(path):
            return
        with open(path, encoding="utf-8") as f:
            data = json.load(f)
        self.stars = {int(k): v for k, v in data.get("stars", {}).items()}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest py_nanobruijn/test_teaching.py::TestGameSession -q`
Expected: PASS（4 passed）

- [ ] **Step 5: Commit**

```bash
git add py_nanobruijn/teaching/game.py py_nanobruijn/test_teaching.py
git commit -m "feat(teaching): GameSession state machine (stars/unlock/save)"
```

---

### Task 3: 世界内容（6 个 .game 文件）+ 标准解验证测试

**Files:**
- Create: `py_nanobruijn/worlds/and.game`、`or.game`、`not.game`、`exists.game`、`iff.game`、`combo.game`
- Test: `py_nanobruijn/test_teaching.py`（新增 `TestWorldContent` + `TestWorldSolutions`）
- 文档: `tutorials/prove-playbook.md` 本任务同步提供设计意图（Task 6 展开重写）

**Interfaces:**
- Consumes: Task 1 的 `GameLoader`；现有 `ProofState`/`run_tactic`/`ProofDone`
- Produces: 6 个可加载的 `.game` 文件（每关 goal/hints/solution/ban）；`WORLDS_DIR` 约定 `py_nanobruijn/worlds/`

- [ ] **Step 1: 写失败测试（内容 + 标准解验证）**

```python
class TestWorldContent:
    """加载全部 6 个世界，结构合法。"""

    def test_load_all_worlds(self):
        import glob
        from py_nanobruijn.teaching.game import GameLoader
        paths = sorted(glob.glob(os.path.join(
            os.path.dirname(__file__), "worlds", "*.game")))
        assert len(paths) == 6
        ids = []
        for p in paths:
            g = GameLoader().load(p)
            ids.append(g.world_id)
            assert len(g.levels) == 5
            assert all(lv.goal and lv.solution for lv in g.levels)
        assert sorted(ids) == ["And", "Combo", "Exists", "Iff", "Not", "Or"]


class TestWorldSolutions:
    """每关标准解必须真实可证（ProofState + run_tactic 逐行跑）。"""

    @pytest.mark.parametrize("world", ["And", "Or", "Not", "Exists", "Iff", "Combo"])
    def test_every_level_solution(self, world):
        import glob
        from py_nanobruijn.teaching.game import GameLoader
        from py_nanobruijn.teaching.proof import ProofState
        from py_nanobruijn.teaching.tactics import ProofDone, run_tactic
        from py_nanobruijn.teaching.parser import parse_expr
        path = os.path.join(os.path.dirname(__file__), "worlds", f"{world.lower()}.game")
        g = GameLoader().load(path)
        core = make_bootstrap()
        for lv in g.levels:
            state = ProofState(core, parse_expr(core, lv.goal))
            for line in lv.solution:
                run_tactic(state, line)
            with pytest.raises(ProofDone):
                run_tactic(state, "done")
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest py_nanobruijn/test_teaching.py::TestWorldContent py_nanobruijn/test_teaching.py::TestWorldSolutions -q`
Expected: FAIL（worlds/ 不存在）

- [ ] **Step 3: 写 6 个世界关卡（goal 见 spec §5，此处给完整内容与标准解）**

**and.game**（`cases` 在 L2/L3 之前禁用——强制投影/嵌套 apply；L5 不 ban）：

```
world And
title 合取世界：从构造到分解
intro 你面前是合取的世界。目标是以 a → b → a ∧ b 为起点的所有通路：
  构造它（And.intro），拆开它（And.right / And.left，以及 cases）。
level 1
name 初见合取
goal: forall (a : Prop), forall (b : Prop), a -> b -> And a b
hint: 目标头部是 And —— 先 apply And.intro
hint: 剩下两个子目标 a 和 b：第一个 intro 引入后 exact，第二个同理
solution:
intro a
intro b
intro ha
intro hb
apply And.intro
exact ha
exact hb
---
level 2
name 只取右半
goal: forall (a : Prop), forall (b : Prop), And a b -> b
ban: cases
hint: 结论是 b，但前提是 And a b。本关禁用 cases —— 用投影 And.right
hint: apply And.right 后剩下目标 And a b，intro 引入后 exact
solution:
intro a
intro b
apply And.right
intro h
exact h
---
level 3
name 结合律
goal: forall (a : Prop), forall (b : Prop), forall (c : Prop), And (And a b) c -> And a (And b c)
hint: 结论是 And a (And b c) —— 先 apply And.intro 拆成 a 和 And b c 两个目标
hint: 前提 And (And a b) c 里也有嵌套：cases h 得到 ha : And a b 与 hc : c
hint: 第一目标 a：cases ha 取 haa；第二目标 And b c：再 apply And.intro
solution:
intro a
intro b
intro c
apply And.intro
intro h
cases h as hab hc
cases hab as ha hb
exact ha
apply And.intro
exact hb
exact hc
---
level 4
name 交换律
goal: forall (a : Prop), forall (b : Prop), And a b -> And b a
hint: 和上一关同样的模式：apply And.intro 拆目标，cases 拆前提
solution:
intro a
intro b
intro h
cases h as ha hb
apply And.intro
exact hb
exact ha
---
level 5
name 矛盾：不可兼得
goal: forall (a : Prop), forall (b : Prop), Not a -> Not (And a b)
hint: Not a 就是 a -> False。目标 Not (And a b) 也是函数类型：intro
hint: 引入 h : And a b 后目标是 False —— cases h 分解出 ha : a，矛盾出现
hint: 用 hna ha 得到 False，exact 收工
solution:
intro a
intro b
intro hna
intro h
cases h as ha hb
exact hna ha
```

**or.game**（L1/L2 练习 Or.inl/inr；L3 用 cases 体验 Or.rec 本质——apply 不支持 recursor 头部；L4/L5 cases）：

```
world Or
title 析取世界：选择的两条路
intro 析取 a ∨ b 的证明是「要么左边要么右边」：Or.inl 走左路，Or.inr 走右路。
  从前提里用 a ∨ b 时，必须两条路都铺好（Or.rec 的本质）。
level 1
name 左路
goal: forall (a : Prop), forall (b : Prop), a -> Or a b
hint: 目标头部是 Or —— apply Or.inl 选择左路
hint: 剩下目标 a，intro 后 exact
solution:
intro a
intro b
intro ha
apply Or.inl
exact ha
---
level 2
name 右路
goal: forall (a : Prop), forall (b : Prop), b -> Or a b
hint: 目标头部是 Or —— apply Or.inr 选择右路
hint: 剩下目标 b，intro 后 exact
solution:
intro a
intro b
intro hb
apply Or.inr
exact hb
---
level 3
name 消去律：两条路都要走
goal: forall (a : Prop), forall (b : Prop), forall (c : Prop), Or a b -> (a -> c) -> (b -> c) -> c
hint: 前提 Or a b 必须两条路都走：cases h 分两路
hint: 左路 ha : a 用 h1，右路 hb : b 用 h2 —— 这就是 Or.rec 的本质，cases 替你写好了
solution:
intro a
intro b
intro c
intro h
intro h1
intro h2
cases h as ha hb
exact h1 ha
exact h2 hb
---
level 4
name 交换律
goal: forall (a : Prop), forall (b : Prop), Or a b -> Or b a
hint: 前提 Or a b 用 cases 分两条路；每条路都用 apply 指向对应方向
solution:
intro a
intro b
intro h
cases h as ha hb
apply Or.inr
exact ha
apply Or.inl
exact hb
---
level 5
name 析取消去与矛盾
goal: forall (a : Prop), forall (b : Prop), Or a b -> Not a -> b
hint: cases h 分两路：左路 ha : a 与 hna 矛盾；右路 hb : b 直接 exact
solution:
intro a
intro b
intro h
intro hna
cases h as ha hb
exact hna ha
exact hb
```

**not.game**（前四关 no ban——Not 是函数类型，intro/exact 足够；L5 ban cases 强制 hand-apply；L4 与已有 mt 等价）：

```
world Not
title 否定世界：把不可能写出来
intro 否定 ¬a 不是魔法，它就是 a → False：一个把 a 的证明变成荒谬的函数。
  这一关的世界里，你会学会用这个定义思考。
level 1
name 否定的定义
goal: forall (a : Prop), (a -> False) -> Not a
hint: 目标 Not a 展开就是 a -> False：intro 即可
hint: 把前提 h 的证明 exact 上去
solution:
intro a
intro h
intro ha
exact h ha
---
level 2
name 双重否定引入
goal: forall (a : Prop), a -> Not (Not a)
hint: 目标 Not (Not a) 展开是 (Not a) -> False：intro h
hint: 现在目标是 False，而 h : Not a 即 a -> False —— h ha 就是 False
solution:
intro a
intro ha
intro h
exact h ha
---
level 3
name 三重否定弱化
goal: forall (a : Prop), Not (Not (Not a)) -> Not a
hint: 目标 Not a：intro hna，目标 False
hint: 前提 h : Not (Not (Not a)) 需要 Not (Not a) 的证明 —— 由 a 的双重否定引入可得
hint: 构造 fun (hnn : Not (Not a)) => h (hnn (fun (h0 : a) => hnn hna)) 太长？直接按思路写：
  内层需要 Not (Not a) -> False，而 hnn hna : False ✓
solution:
intro a
intro h
intro hna
exact h (fun (hnn : Not (Not a)) => hnn hna)
---
level 4
name 逆否：mt 的手写
goal: forall (a : Prop), forall (b : Prop), (a -> b) -> Not b -> Not a
hint: 目标 Not a：intro hna，目标 False
hint: 前提有 hab : a -> b 与 hnb : Not b —— hnb (hab hna) 就是 False
solution:
intro a
intro b
intro hab
intro hnb
intro hna
exact hnb (hab hna)
---
level 5
name 析取的否定：拆开它
goal: forall (a : Prop), forall (b : Prop), Not (Or a b) -> Not a
ban: cases
hint: 目标 Not a：intro hna，目标 False
hint: 前提 h : Not (Or a b) —— 构造 Or a b 的证明（Or.inl hna）交给 h 即可
solution:
intro a
intro b
intro h
intro hna
exact h (Or.inl a b hna)
```

**exists.game**（universe 参数注意：`Prop : Sort 1`，Exists 需 `.{1}`；L2/L3/L4 用 `p : Prop -> Prop`）：

```
world Exists
title 存在世界：给出证据
intro 存在命题 ∃x, p x 的证明是一个「证人」：x 以及 p x 的证明。
  反向使用（从 ∃x, p x 里取证人）则是 cases 的领地。
level 1
name 平凡的存在
goal: @Exists.{1} Prop (fun (x : Prop) => x) -> True
hint: 结论是 True —— 直接 exact True.intro，前提甚至可以不用
hint: 如果想练习分解：cases h 得到 x : Prop 与 hx : x，然后 exact True.intro
solution:
intro h
exact True.intro
---
level 2
name 证人传递（Exists.imp 方向一）
goal: forall (p : Prop -> Prop), forall (q : Prop -> Prop), forall (h : forall (x : Prop), p x -> q x), @Exists.{1} Prop p -> @Exists.{1} Prop q
hint: 前提 e : Exists p —— cases e 取出证人 x 与 hx : p x
hint: 目标 Exists q：exact @Exists.intro.{1} Prop q x (h x hx) 一步给出证人（apply 只支持常量，recursor/构造子带参用 exact）
solution:
intro p
intro q
intro h
intro e
cases e as x hx
exact @Exists.intro.{1} Prop q x (h x hx)
---
level 3
name 矛盾的否定（not_exists 方向）
goal: forall (p : Prop -> Prop), (forall (x : Prop), p x -> False) -> @Exists.{1} Prop p -> False
hint: cases e 取证人 x 与 hx : p x
hint: h x hx : False —— 直接 exact
solution:
intro p
intro h
intro e
cases e as x hx
exact h x hx
---
level 4
name 冗余的包装
goal: forall (p : Prop -> Prop), @Exists.{1} Prop (fun (x : Prop) => p x) -> @Exists.{1} Prop p
hint: 左边 Exists (fun x => p x) 分解出 x 与 (fun x => p x) x —— 后者就是 p x
hint: exact @Exists.intro.{1} Prop p x hx 一步给出证人
solution:
intro p
intro e
cases e as x hx
exact @Exists.intro.{1} Prop p x hx
---
level 5
name 取出与给出
goal: forall (p : Prop -> Prop), forall (a : Prop), @Exists.{1} Prop p -> p a -> True
hint: 结论 True：直接 exact True.intro
hint: 想练习分解就 cases e 看看证人在哪
solution:
intro p
intro a
intro e
intro hpa
exact True.intro
```

**iff.game**（L1/L2/L3 no ban——apply/exact 组合；L4 复习；L5 ban cases 强制 apply）：

```
world Iff
title 等价世界：两个方向的约定
intro 等价 a ↔ b 是两座桥：Iff.mp 从 a 到 b，Iff.mpr 从 b 到 a。
  Iff.intro 则同时铺两条路。
level 1
name 自反
goal: forall (a : Prop), Iff a a
hint: apply Iff.intro 后两个目标都是 a -> a
solution:
intro a
apply Iff.intro
intro ha
exact ha
intro ha
exact ha
---
level 2
name 对称
goal: forall (a : Prop), forall (b : Prop), Iff a b -> Iff b a
hint: apply Iff.intro 拆两个方向
hint: 第一个方向 b -> a：intro hb 后 exact Iff.mpr a b h hb
hint: 第二个方向 a -> b：intro ha 后 exact Iff.mp a b h ha
solution:
intro a
intro b
apply Iff.intro
intro hb
exact Iff.mpr a b h hb
intro ha
exact Iff.mp a b h ha
---
level 3
name 传递
goal: forall (a : Prop), forall (b : Prop), forall (c : Prop), Iff a b -> Iff b c -> Iff a c
hint: 和对称同模式：Iff.intro 拆两方向，各用 mp/mpr 组合
hint: a -> c：Iff.mp b c hbc (Iff.mp a b hab ha)
hint: c -> a：Iff.mpr a b hab (Iff.mpr b c hbc hc)
solution:
intro a
intro b
intro c
apply Iff.intro
intro hab
intro hbc
intro ha
exact Iff.mp b c hbc (Iff.mp a b hab ha)
intro hc
exact Iff.mpr a b hab (Iff.mpr b c hbc hc)
---
level 4
name 逆方向构造
goal: forall (a : Prop), forall (b : Prop), (a -> b) -> (b -> a) -> Iff a b
hint: apply Iff.intro 后第一个方向直接 exact h1，第二个 exact h2
solution:
intro a
intro b
intro h1
intro h2
apply Iff.intro
exact h1
exact h2
---
level 5
name 等价保持否定
goal: forall (a : Prop), forall (b : Prop), Iff a b -> Iff (Not a) (Not b)
hint: apply Iff.intro 拆两方向，每个方向都是逆否（mt 的模式）
hint: Not a -> Not b：intro hna，再 intro hb，矛盾 hna (Iff.mpr a b h hb)
solution:
intro a
intro b
apply Iff.intro
intro h
intro hna
intro hb
exact hna (Iff.mpr a b h hb)
intro hnb
intro ha
exact hnb (Iff.mp a b h ha)
```

**combo.game**（综合世界，全部技巧）：

```
world Combo
title 综合世界：第一公里终点
intro 前五个世界的技巧在这里汇合。每一关都是一道完整的谜题。
  提示会越来越稀疏——是时候自己铺路了。
level 1
name 分配律反向
goal: forall (a : Prop), forall (b : Prop), forall (c : Prop), Or (And a b) (And a c) -> And a (Or b c)
hint: 前提是析取：cases 分两路（h1 : And a b / h2 : And a c）
hint: 左路：apply And.intro 拆目标为 a 和 Or b c，分别用 And.left/And.right 投影 h1
hint: 右路同理（投影 h2）
solution:
intro a
intro b
intro c
intro h
cases h as h1 h2
apply And.intro
exact (@And.left a b h1)
exact (@And.right a b h1)
apply And.intro
exact (@And.left a c h2)
exact (@And.right a c h2)
---
level 2
name De Morgan 全量版
goal: forall (a : Prop), forall (b : Prop), Iff (Not (Or a b)) (And (Not a) (Not b))
hint: Iff.intro 拆两方向
hint: 方向一：intro h 后 apply And.intro，两个目标各用 h (Or.inl ...) / h (Or.inr ...)
hint: 方向二：intro h 后 intro x，cases x 分两路，各用 And.left/And.right 投影
solution:
intro a
intro b
apply Iff.intro
intro h
apply And.intro
intro ha
exact h (Or.inl a b ha)
intro hb
exact h (Or.inr a b hb)
intro h
intro x
cases x as x1 x2
exact (@And.left (Not a) (Not b) h) x1
exact (@And.right (Not a) (Not b) h) x2
---
level 3
name 函数与合取
goal: forall (a : Prop), forall (b : Prop), forall (c : Prop), (Or a b -> c) -> And (a -> c) (b -> c)
hint: apply And.intro 拆两目标
hint: 第一个 a -> c：intro ha 后 exact h (Or.inl a b ha)
hint: 第二个 b -> c：intro hb 后 exact h (Or.inr a b hb)
solution:
intro a
intro b
intro c
apply And.intro
intro h
intro ha
exact h (Or.inl a b ha)
intro hb
exact h (Or.inr a b hb)
---
level 4
name 合取的构造与应用
goal: forall (a : Prop), forall (b : Prop), forall (c : Prop), And (a -> b) (a -> c) -> a -> And b c
hint: intro h1 h2 h3 —— 前提 h1 : And (a -> b) (a -> c)，h3 : a
hint: apply And.intro 后两个目标：And.left h1 h3 与 And.right h1 h3
solution:
intro a
intro b
intro c
intro h1
intro h3
apply And.intro
exact (@And.left (a -> b) (a -> c) h1) h3
exact (@And.right (a -> b) (a -> c) h1) h3
---
level 5
name 排中律的直觉主义版
goal: forall (a : Prop), Not (Not (Or a (Not a)))
hint: 经典排中律 a ∨ ¬a 直觉主义证不出来——但双重否定可以
hint: intro h 后目标 False。h 需要 Or a (Not a) 的证明：Or.inr 构造右路，其参数是 Not a
hint: Not a 就是 a -> False：用 Function.comp 把 h 和 Or.inl 组合（见 not_not_em）
solution:
intro a
intro h
exact h (Or.inr a (Not a) (@Function.comp.{0, 0, 0} a (Or a (Not a)) False h (@Or.inl a (Not a))))
```

- [ ] **Step 4: 跑标准解验证测试，迭代修正内容直到全绿**

Run: `.venv/bin/python -m pytest py_nanobruijn/test_teaching.py::TestWorldContent py_nanobruijn/test_teaching.py::TestWorldSolutions -q`
Expected: PASS（7 个测试：1 结构 + 6 世界参数化）
若有 FAIL：按报错（`ValueError: done: ...`/`ParseError`）修正对应关卡的 goal 或 solution——注意教学语法约束（binder 注解、`@Const`、`.{1}` 层级、Or.rec 的 level 参数在 apply 时自动匹配）

- [ ] **Step 5: Commit**

```bash
git add py_nanobruijn/worlds/ py_nanobruijn/test_teaching.py .gitignore
git commit -m "feat(teaching): six .game worlds (And/Or/Not/Exists/Iff/Combo) with verified solutions"
```

---

### Task 4: REPL 集成（#game / #worlds / 关卡循环）

**Files:**
- Modify: `py_nanobruijn/teaching/repl.py`
- Test: `py_nanobruijn/test_teaching.py`（新增 `class TestGameRepl`）

**Interfaces:**
- Consumes: Task 1-3（`GameLoader`/`GameSession`/`Game`/`Level`、worlds/ 文件）；现有 `ProofState`/`run_tactic`/`ProofDone`/`AbortProof`
- Produces: `Repl` 新命令 `#worlds`/`#game [世界]`；`Repl.start_game(world_id: str) -> str | None`（错误返回文本）；`_GameSession` 信号；关卡内 `hint`/`solution` 命令与 ban 生效；`_run_proof(..., level, game)` 返回 `"completed"`/`"abandoned"`

- [ ] **Step 1: 写失败测试**

```python
class TestGameRepl:
    """REPL 游戏模式集成。"""

    def make_repl(self):
        from py_nanobruijn.teaching.game import GameLoader, GameSession
        worlds_dir = os.path.join(os.path.dirname(__file__), "worlds")
        path = os.path.join(worlds_dir, "and.game")
        game = GameLoader().load(path)
        repl = Repl(make_bootstrap())
        repl.pending_game = GameSession(game, saves_dir=None)
        return repl

    def test_worlds_lists(self):
        import glob
        r = Repl(make_bootstrap())
        out = r.process_line("#worlds")
        assert "And" in out and "Combo" in out and len(out.splitlines()) >= 6

    def test_game_unknown_world(self):
        r = Repl(make_bootstrap())
        out = r.process_line("#game Bogus")
        assert "未知世界" in out

    def test_enter_game_signal(self):
        r = self.make_repl()
        with pytest.raises(Exception) as exc:
            r.process_line("#game And")
        assert type(exc.value).__name__ == "_GameSession"

    def test_level_hint_and_solution_lines(self):
        r = self.make_repl()
        r.pending_game = None
        with pytest.raises(Exception) as exc:
            r.process_line("#game And")
        session = exc.value.session
        from py_nanobruijn.teaching.proof import ProofState
        from py_nanobruijn.teaching.parser import parse_expr
        from py_nanobruijn.teaching.repl import _GameSession
        assert isinstance(session, _GameSession)
        # 关卡 1 的 hint 依次显示
        level = session.session.game.level(1)
        assert level.hints[0] == "目标头部是 And —— 先 apply And.intro"

    def test_ban_reported(self):
        from py_nanobruijn.teaching.game import Game, Level
        from py_nanobruijn.teaching.repl import Repl
        g = Game("X", "t", "i", [Level(1, "L", "forall (a : Prop), a -> a",
                                      [], ["intro a"], ["exact"])])
        from py_nanobruijn.teaching.game import GameSession
        from py_nanobruijn.teaching.proof import ProofState
        from py_nanobruijn.teaching.parser import parse_expr
        state = ProofState(make_bootstrap(), parse_expr(make_bootstrap(), "forall (a : Prop), a -> a"))
        r = Repl(make_bootstrap())
        out = r._game_tactic_check(g.level(1), "exact a")
        assert "禁用" in out
        assert r._game_tactic_check(g.level(1), "intro a") is None
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest py_nanobruijn/test_teaching.py::TestGameRepl -q`
Expected: FAIL（`#worlds` unknown command）

- [ ] **Step 3: 实现 REPL 集成**

repl.py 修改（保持 `#prove` 行为不变）：

```python
class _GameSession(Exception):
    """process_line 的内部信号：进入 #game 游戏子循环（由 run() 捕获）。"""

    def __init__(self, session):
        super().__init__("game session")
        self.session = session
```

- 命令分发：`_command` 增加 `cmd == "worlds"` → `self._worlds()`；`cmd == "game"` → `self._game(rest)`。
- 新增方法：

```python
    STEP_TACTICS = ('intro', 'apply', 'exact', 'cases')

    def _worlds(self) -> str:
        import glob
        lines = []
        for path in sorted(glob.glob(os.path.join(
                os.path.dirname(os.path.dirname(__file__)), "worlds", "*.game"))):
            game = GameLoader().load(path)
            prog = GameSession(game)
            prog.load_progress()
            stars = f"{len(prog.stars)}/5 关"
            lines.append(f"{game.world_id} — {game.title}（{stars}）")
        return "\n".join(lines) or "（暂无世界，worlds/ 目录为空）"

    def _game(self, text: str) -> str:
        text = text.strip()
        if not text:
            return "usage: #game <世界>（如 #game And；#worlds 查看全部）"
        err = self.start_game(text)
        if err:
            return self._error(err)
        raise _GameSession(self.pending_game)

    def start_game(self, world_id: str) -> str | None:
        """加载世界并构造会话（失败返回错误文本）。"""
        import glob
        path = os.path.join(os.path.dirname(os.path.dirname(__file__)),
                            "worlds", world_id.lower() + ".game")
        if not os.path.exists(path):
            known = sorted(os.path.basename(p)[:-5] for p in glob.glob(
                os.path.join(os.path.dirname(os.path.dirname(__file__)),
                             "worlds", "*.game")))
            return f"未知世界 {world_id!r}（可选：{', '.join(known)}）"
        game = GameLoader().load(path)
        session = GameSession(game)
        session.load_progress()
        self.pending_game = session
        return None
```

- `run()` 开头：进入游戏子循环（与 `_ProveSession` 同模式，但要处理"启动时已有 pending_game"）：

```python
    def run(self, stdin=None, stdout=None) -> int:
        ...
        session_path = self._open_session(stdout)
        while True:
            try:
                if self.pending_game is not None:
                    game = self.pending_game
                    self.pending_game = None
                    self._run_game(game, stdin, stdout, session_path)
                    continue
                prompt = ...
```

- 关卡循环 `_run_game` + 改造 `_run_proof`：

```python
    def _run_game(self, game, stdin, stdout, session_path) -> None:
        print(f"{game.title}", file=stdout)
        print(f"{game.intro}", file=stdout)
        while True:
            no = game.next_unfinished()
            if no is None:
                print(self._c("🎉 世界通关！全部关卡完成。", "green"), file=stdout)
                return
            level = game.level(no)
            print(f"\n{self._c(f'第 {no} 关：{level.name or ""}', 'green')}", file=stdout)
            try:
                goal_ty = parse_expr(self.core, level.goal)
                state = ProofState(self.core, goal_ty, self.timeout_secs, self.color)
            except (ValueError, ParseError) as err:
                print(self._error(f"关卡 goal 解析失败: {err}"), file=stdout)
                return
            game.current_level_no = no
            status = self._run_proof(state, stdin, stdout, session_path,
                                     level=level, game=game)
            if status == "abandoned":
                return

    def _game_tactic_check(self, level, line: str) -> str | None:
        head = line.strip().split(maxsplit=1)[0].rstrip(';')
        if head in level.bans:
            return f"本关禁用 {head}——换一条路想想（如 apply/exact 直接构造）"
        return None
```

- `_run_proof` 加参数 `level=None, game=None`，循环内：

```python
        steps = 0
        used_hint = False
        hint_idx = 0
        if level is not None:
            print(f"目标: {pretty(self.core, state.goal_ty, self.color)}", file=stdout)
            print(state.context(), file=stdout)
        while True:
            ...
            if level is not None:
                cmd = line.strip()
                if cmd == "hint":
                    if hint_idx < len(level.hints):
                        hint = level.hints[hint_idx]
                        hint_idx += 1
                        used_hint = True
                        print(f"提示 {hint_idx}/{len(level.hints)}: {hint}", file=stdout)
                    else:
                        print("没有更多提示了——再想想，或者 solution 看标准解", file=stdout)
                    continue
                if cmd == "solution":
                    print("标准解：", file=stdout)
                    print("\n".join(level.solution), file=stdout)
                    print(self._c("（本关未通关。quit 或继续尝试）", "gray"), file=stdout)
                    return "abandoned"
                blocked = self._game_tactic_check(level, line)
                if blocked:
                    print(self._error(blocked), file=stdout)
                    continue
                if line.strip().split(maxsplit=1)[0] in self.STEP_TACTICS:
                    steps += 1
            try:
                out = run_tactic(state, line)
            except ProofDone as done:
                print(done.text, file=stdout)
                if level is not None and game is not None:
                    stars = game.complete(steps, used_hint)
                    print(self._c(f"过关！获得 {'★' * stars}{'☆' * (3 - stars)}", 'green'), file=stdout)
                    if level.solution:
                        print("标准解（你的路径可能不同，两种都正确）：", file=stdout)
                        print("\n".join(level.solution), file=stdout)
                return "completed"
            except AbortProof:
                return "abandoned"
            ...
```

注意 `_run_proof` 原签名 `(self, state, stdin, stdout, session_path)`——加 keyword-only 默认参数保持兼容（现有 `run()` 的调用不变）。原 `_run_proof` 在 `_ProveSession` 路径的返回 None 现在返回 "completed"——调用处（run 主循环 `self._run_proof(session.state, stdin, stdout, session_path)`）忽略返回值即可。

- **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest py_nanobruijn/test_teaching.py::TestGameRepl -q`
Expected: PASS（5 passed）

- [ ] **Step 5: 手动冒烟：完整通关一关**

Run:
```bash
printf '#game And\nintro a\nintro b\napply And.intro\nintro ha\nexact ha\nintro hb\nexact hb\ndone\n#quit\n' | .venv/bin/python -m py_nanobruijn repl
```
Expected: 第 1 关目标显示 → tactic 执行 → 过关 + ★ 星级 + 标准解 → 自动进入第 2 关 → `#quit` 退出

- [ ] **Step 6: Commit**

```bash
git add py_nanobruijn/teaching/repl.py py_nanobruijn/test_teaching.py
git commit -m "feat(teaching): REPL game mode (#game/#worlds, hints, bans, stars)"
```

---

### Task 5: CLI 非交互参数（--script / --json / --game）

**Files:**
- Modify: `py_nanobruijn/__main__.py`、`py_nanobruijn/teaching/repl.py`
- Test: `py_nanobruijn/test_teaching.py::TestCli` 扩展

**Interfaces:**
- Consumes: Task 4 的 `Repl`（`start_game`/`pending_game`/`run`）
- Produces: `py-nanobruijn repl --script <文本>`（非交互执行后退出）；`--json`（机器可读包装）；`--game <世界>`（启动即进入世界）

- [ ] **Step 1: 写失败测试**

```python
class TestCliGame:
    def test_cli_script_check(self):
        import subprocess
        import sys
        proc = subprocess.run(
            [sys.executable, "-m", "py_nanobruijn", "repl", "--script",
             "#check forall (a : Prop), a -> a"],
            capture_output=True, text=True, timeout=30, check=False)
        assert proc.returncode == 0
        assert "forall (a : Prop), a -> a" in proc.stdout
        assert "error" not in proc.stdout.lower()

    def test_cli_script_json(self):
        import subprocess
        import sys
        import json as _json
        proc = subprocess.run(
            [sys.executable, "-m", "py_nanobruijn", "repl", "--script",
             "#check Prop", "--json"],
            capture_output=True, text=True, timeout=30, check=False)
        assert proc.returncode == 0
        data = _json.loads(proc.stdout)
        assert data["ok"] is True
        assert "Prop" in data["output"]

    def test_cli_script_prove_solution(self):
        import subprocess
        import sys
        script = ("#prove forall (a : Prop), a -> a\n"
                  "intro a\n"
                  "intro ha\n"
                  "exact ha\n"
                  "done\n")
        proc = subprocess.run(
            [sys.executable, "-m", "py_nanobruijn", "repl", "--script", script],
            capture_output=True, text=True, timeout=30, check=False)
        assert "内核检查: 通过" in proc.stdout

    def test_cli_script_error_nonzero(self):
        import subprocess
        import sys
        proc = subprocess.run(
            [sys.executable, "-m", "py_nanobruijn", "repl", "--script", "#check bogus"],
            capture_output=True, text=True, timeout=30, check=False)
        assert proc.returncode != 0

    def test_cli_game_flag(self):
        import subprocess
        import sys
        proc = subprocess.run(
            [sys.executable, "-m", "py_nanobruijn", "repl", "--game", "And"],
            input="#quit\n", capture_output=True, text=True, timeout=30, check=False)
        assert "合取世界" in proc.stdout
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest py_nanobruijn/test_teaching.py::TestCliGame -q`
Expected: FAIL（`--script` unrecognized arguments）

- [ ] **Step 3: 实现 CLI 参数**

`__main__.py`：

```python
    repl = subcommands.add_parser("repl", help="interactive teaching REPL")
    repl.add_argument("--timeout", type=float, default=5.0, ...)
    repl.add_argument("--no-color", ...)
    repl.add_argument("--color", ...)
    repl.add_argument("--script", help="non-interactive: run these lines then exit")
    repl.add_argument("--json", action="store_true",
                      help="with --script: machine-readable JSON output")
    repl.add_argument("--game", help="enter a world on startup")
```

`_run_repl`：

```python
def _run_repl(args: argparse.Namespace) -> int:
    import io
    from .teaching.core import make_bootstrap
    from .teaching.repl import Repl
    core = make_bootstrap()
    color = True if args.color else (False if args.no_color else None)
    repl = Repl(core, timeout_secs=args.timeout, color=color)
    if args.game:
        err = repl.start_game(args.game)
        if err:
            print(f"CHECK ERROR: {err}", file=sys.stderr)
            return 1
    if args.script:
        buf = io.StringIO()
        code = repl.run(stdin=io.StringIO(args.script), stdout=buf)
        text = buf.getvalue()
        if args.json:
            print(json.dumps({"ok": code == 0, "output": text},
                             ensure_ascii=False))
        else:
            sys.stdout.write(text)
        return code
    return repl.run()
```

注意：`--script` 时 session 记录仍会写文件（`_open_session`）——可以接受（无状态重放也记录）；若测试不想产生 sessions 文件，可接受（sessions/ 已 gitignore）。

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest py_nanobruijn/test_teaching.py::TestCliGame -q`
Expected: PASS（5 passed）

- [ ] **Step 5: Commit**

```bash
git add py_nanobruijn/__main__.py py_nanobruijn/teaching/repl.py py_nanobruijn/test_teaching.py
git commit -m "feat(cli): repl --script/--json non-interactive mode for agent integration"
```

---

### Task 6: playbook.md 重写为游戏设计文档

**Files:**
- Modify: `tutorials/prove-playbook.md`（重写）

**Interfaces:**
- Consumes: Task 3 的世界内容（关卡列表/标准解/ban 理由）

- [ ] **Step 1: 重写文档**

结构（保留三节教学法总览 + 世界设计文档 + 加关卡指南）：

```markdown
# 🎯 教学剧本：关卡世界设计文档（#prove Playbook）

> 本文档是 GAME 模式的关卡设计文档：md 与 worlds/*.game 双向对照——
> 先在这里设计，再落成 .game 文件，最后用 REPL 验证。
> 配套工具：py-nanobruijn repl 的 #game 模式（关卡闯关）。

## 学习路径（第一公里）

| 节 | 主题 | 世界 | 学完后你能 |
|---|---|---|---|
| 第 1 节 | λ 与类型——认识证明的语言 | Not（前 2 关）+ Iff（前 3 关） | 读懂 fun/∀/->，手写简单证明 |
| 第 2 节 | recursor——遇见结构 | And + Or + Exists | 用 cases 分解结构 |
| 第 3 节 | 独立证明——终点 | Not/Iff 后段 + Combo | 独立完成组合证明 |

## 世界设计（与 worlds/*.game 对照）

### And 世界（5 关）
| 关 | 命题 | 设计意图 | 标准解见 |
|---|---|---|---|
| 1 初见合取 | a -> b -> And a b | And.intro 构造 | worlds/and.game |
| 2 只取右半 | And a b -> b | And.right/left 投影 | worlds/and.game |
| 3 结合律 | ... | 嵌套 cases | worlds/and.game |
...

（每关小节：命题 + hints 设计 + 标准解 + ban 理由，与 .game 完全一致）

### Or / Not / Exists / Iff / Combo 世界（同上模式）

## 加关卡指南
1. 在本文档对应世界小节写关卡（goal + 教学点 + 标准解草稿）
2. 落成 worlds/<world>.game（格式见 game.py 文档字符串）
3. 跑 TestWorldSolutions 验证标准解
4. 手动 #game <世界> 通关体验一次
```

- [ ] **Step 2: 验证双向一致性（对照 .game 文件与文档的关卡数/名字）**

Run: `rg -c "^name " py_nanobruijn/worlds/*.game`（每文件 5 关）与文档表格对照
Expected: 30 关全部出现在文档

- [ ] **Step 3: Commit**

```bash
git add tutorials/prove-playbook.md
git commit -m "docs(tutorials): rewrite playbook as game world design doc"
```

---

### Task 7: opencode skill + tutor agent

**Files:**
- Create: `.opencode/skills/nanobruijn/SKILL.md`、`.opencode/agents/nanobruijn-tutor.md`

**Interfaces:**
- Consumes: Task 5 的 `--script/--json` CLI

- [ ] **Step 1: 写 SKILL.md**

```markdown
---
name: nanobruijn
description: "Verify Lean proofs and grade student submissions with the nanobruijn teaching REPL. Check proof terms, load game levels, and grade proofs via `py-nanobruijn repl --script`. Use for Lean teaching tasks."
---

# nanobruijn 教学 REPL 驱动

用 `py-nanobruijn repl --script` 无状态验证证明（每行新 TypeChecker，适合 agent 调用）。

## 调用规则

| 任务 | 命令 |
|---|---|
| 验证表达式的类型 | `py-nanobruijn repl --script "#check <expr>"` |
| 批改学生证明（#prove 草稿） | `--script` 传 `#prove <命题>` + 每行一个 tactic + `done` |
| 查常量/定理 | `--script "#print <name>"`（axiom/def/theorem 及其值） |
| 游戏关卡可解性 | `--script` 传该关标准解（或 `#game <世界>` 交互） |
| 机器可读输出 | 加 `--json`（{"ok": bool, "output": str}），错误时返回码非 0 |

## 工作流

1. 学生提交证明（文本）→ 包成 `#prove` + tactic 序列 → `--script --json`
2. 读 `ok` 与 `output`：`内核检查: 通过` = 证明正确；`error:` 行 = 失败原因
3. 反馈循环：把 error 信息转成教学提示（提示用中文，指向下一个该用的 tactic）
4. 多轮批改：每次都是新的 `--script` 调用（无状态）

## Anti-patterns

| ❌ 不要 | ✅ 要 |
|---|---|
| 启动交互式 repl（会挂起等输入） | 永远 `--script`（EOF 自动退出） |
| 猜表达式类型 | 先 `#check` |
| 用内核 `check` 命令跑教学表达式 | 走 `repl`（有 NatLit 拦截等友好错误） |
| 让学生用 `h.1`/`h.2` 投影 | 教学语法用 `And.left`/`And.right` |
| 忘 binder 注解写 `fun x => e` | `fun (x : A) => e`（内核要求） |

## 已知限制

- Nat 字面量（如 `#check 42`）v1 教学核心不支持
- 隐式参数不自动填充：`@And True True` 显式传参
- `Prop : Sort 1`：Exists 需 `@Exists.{1} Prop p`
- 超时保护：默认 5.0s，大项加 `--timeout`
```

- [ ] **Step 2: 写 tutor agent**

```markdown
---
description: 批改 Lean 证明的教学代理：验证学生的 #prove 草稿并给出中文提示。
mode: subagent
model: deepseek
permission:
  bash:
    "py-nanobruijn repl *": allow
    "*": deny
---

你是 nanobruijn 的教学助手。学生提交证明草稿（#prove 命题 + tactic 序列）时：
1. 用 `py-nanobruijn repl --script --json` 验证
2. 通过（内核检查: 通过）→ 肯定 + 可选展示标准解
3. 失败 → 把 error 转成中文教学提示（指出目标状态与下一步 tactic），
   不要直接给完整解
4. 只允许运行 py-nanobruijn repl 命令（其他 bash 被拒绝）
```

- [ ] **Step 3: 验证 skill 被 opencode 发现**

Run: `ls .opencode/skills/nanobruijn/ .opencode/agents/`
Expected: 两个文件存在。注意：当前会话已加载的 skill 列表不含它——需要重启 opencode 会话才会出现在 `<available_skills>`（此步只做文件确认，无法在会话内验证发现机制）。

- [ ] **Step 4: Commit**

```bash
git add .opencode/
git commit -m "feat(agent): nanobruijn skill + tutor subagent for opencode"
```

---

### Task 8: 文档收尾 + 全量验证

**Files:**
- Modify: `AGENTS.md`、`py_nanobruijn/teaching/README.md`、`.gitignore`

**Interfaces:**
- Consumes: 全部前述任务

- [ ] **Step 1: 更新文档**

- `.gitignore` 加 `py_nanobruijn/saves/`
- `AGENTS.md`：架构图加 `game.py`/`worlds/`；新增小节「GAME 模式」说明命令与文件格式；CLI 参数补充 `--script/--json/--game`
- `teaching/README.md`：加「闯关模式」章节（`#worlds`/`#game`/`hint`/`solution`/星级说明）

- [ ] **Step 2: 全量验证**

Run: `.venv/bin/python -m pytest py_nanobruijn -q && .venv/bin/ruff check py_nanobruijn`
Expected: 全部通过（含新增 ~30 测试）+ ruff 0 errors

- [ ] **Step 3: 冒烟：完整游戏会话 + agent 通道**

Run:
```bash
printf '#game And\n#quit\n' | .venv/bin/python -m py_nanobruijn repl
.venv/bin/python -m py_nanobruijn repl --script "#check Prop" --json
```
Expected: 世界 intro 显示；JSON 输出 `{"ok": true, ...}`

- [ ] **Step 4: Commit**

```bash
git add AGENTS.md py_nanobruijn/teaching/README.md .gitignore
git commit -m "docs: game mode docs, AGENTS.md updates, saves/ gitignore"
```

---

## Self-Review

**Spec 覆盖检查：**
- §2 GAME 数据结构（.game 格式/字段）→ Task 1 ✓
- §3 状态机（星级/解锁/存档/ban/hint/步数）→ Task 2 + Task 4 ✓
- §4 REPL 集成（#worlds/#game/hint/solution/quit/--game）→ Task 4 + Task 5 ✓
- §5 世界内容（6 世界 × 5 关，spec 表逐关对照）→ Task 3 ✓
- §6 playbook 重定位 → Task 6 ✓
- §7 opencode skill（--script/--json/SKILL.md/agent）→ Task 5 + Task 7 ✓
- §8 测试策略 → 各任务 TDD 步骤 + Task 8 全量 ✓

**类型一致性：** `GameLoader.load` / `Game` / `Level` / `GameSession`（`stars`/`complete`/`next_unfinished`/`unlocked`/`save`/`load_progress`）/ `Repl.start_game`/`pending_game`/`_GameSession` 在 Task 1-5 中签名一致；`_run_proof(..., level=None, game=None)` 返回 `"completed"`/`"abandoned"` 在 Task 4 定义并唯一使用 ✓

**注意点：** Task 1 的 `solution` 收集模式在 Step 3 有修正提示（`in_solution` 状态）；Task 4 的 `_run_proof` 修改必须保持现有 `#prove` 行为（level=None 路径与原逻辑逐行相同）——Step 5 冒烟测试专门覆盖。